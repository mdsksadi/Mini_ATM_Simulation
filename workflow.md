# Our Workflow
1. Create a repo in gitHub    
    - folder = repository = repo = directory   
    - in git/gitHub repository are tow types:   
        - local repository   
        - remote repository 
    